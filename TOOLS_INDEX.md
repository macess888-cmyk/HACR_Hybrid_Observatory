# HACR Hybrid Observatory — Tools Index

This index maps the current hostile-runtime instrumentation stack.

## Replay + Receipts

- `RUN_PIPELINE.bat`
- `tools/runtime_pipeline_runner/`
- `tools/receipt_integrity_verifier/`
- `tools/receipt_hashing/`
- `Receipts/`

Purpose:
deterministic replay, receipt verification, SHA256 sealing, and reproducibility artifacts.

---

## Runtime State + Timeline

- `tools/runtime_state_renderer/`
- `tools/runtime_reconstruction_timeline_engine/`

Purpose:
render runtime progression and interruption reconstruction viability across time.

---

## Reconstruction + Governability

- `tools/interruption_reconstruction_viability_harness/`
- `tools/governability_mirage_visualizer/`
- `tools/runtime_governability_decay_index/`

Purpose:
detect divergence between governability appearance and executable interruption viability.

---

## Topology + Continuation

- `tools/continuation_hardening_topology_renderer/`
- `tools/continuation_hardening_heatmap/`
- `tools/locality_fragmentation_mapper/`
- `tools/locality_traversal_visualizer/`

Purpose:
inspect continuation hardening, topology pressure, locality fragmentation, and traversal viability.

---

## CARE / ASRO Seam

- `tools/care_asro_seam_integrity_tester/`

Purpose:
verify that evidence continuity remains evidence only and does not inherit executable interruption legitimacy.

---

## Visuals

- `docs/hostile_runtime_diagrams/`

Purpose:
SVG diagrams for hostile-runtime state evolution, locality flow, and traversal sequences.

---

## Boundary

All tools are:

- observer-only
- runtime-local
- deterministic
- anti-authoritative
- non-remedial
- hostile-runtime testable

They do NOT:

- authorize interruption
- govern execution
- prescribe remediation
- certify governability
- or create operational authority.

UNKNOWN -> HOLD.

Break survivability, not ontology.