# Compression Failure Boundary Tester

Observer-only anti-inflation tester for checking whether repeated survivability observations should remain compressed inside existing inspection geometry or become an expansion candidate.

## Core Reductions

```text
recurrence != distinction
persistent survivability != new object
compression failure, not recurrence, justifies expansion