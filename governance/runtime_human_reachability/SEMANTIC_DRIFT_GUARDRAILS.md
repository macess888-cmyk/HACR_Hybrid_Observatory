# Semantic Drift Guardrails

The observatory must not allow:

- survivability -> legitimacy drift
- asymmetry -> inevitability drift
- persistence -> closure drift
- visibility -> authority drift
- instrumentation -> admissibility drift

Detected semantic inheritance pressure:
UNKNOWN -> HOLD.

The observatory measures runtime conditions only.

The observatory does not create bind authority.

Evidence does not create admissibility.